import 'package:flutter/material.dart';

class AppStyles {
  static ThemeMode themeMode = ThemeMode.light;
  static const mainPadding = EdgeInsets.symmetric(vertical: 16, horizontal: 24);
  static const mainPaddingSkeleton = EdgeInsets.symmetric(vertical: 20, horizontal: 36);
  static const borderRadiusApp = 12.0;
}
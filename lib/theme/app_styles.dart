import 'package:flutter/material.dart';

class AppStyles {
  static ThemeMode themeMode = ThemeMode.light;
  static const paddingListView = EdgeInsets.symmetric(vertical: 16, horizontal: 10);
  static const paddingScreen = EdgeInsets.symmetric(vertical: 16, horizontal: 24);
  static const borderRadiusApp = 12.0;
}